providers = ["php"]

[phases.setup]
nixPkgs = ["php83", "composer", "nodejs"]

[phases.install]
cmds = ['composer install --no-dev --ignore-platform-reqs', 'npm i']

[phases.build]
cmds = ['npm run build']