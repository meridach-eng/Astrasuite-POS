providers = ["php"]

[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "nodejs"]

[phases.install]
cmds = ['composer install --no-dev --ignore-platform-reqs', 'npm i']

[phases.build]
cmds = ['npm run build']